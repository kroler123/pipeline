#!groovy
// Check properties
properties([disableConcurrentBuilds()])

pipeline {
	agent {
		label 'master'
	}
	options {
		buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
		timestamps()
		skipStagesAfterUnstable()
	}
	stages {
                stage("first step") {
                        steps {
				script {
					GET_VAR = sh (
						script: 'ssh -i \'/var/lib/jenkins/.ssh/ats_rsa\' root@10.20.1.20 \'hostname\'',
						returnStdout: true
					).trim()
				}
				echo "hostname asterisk = ${GET_VAR} !"
                        }
                }
		stage("Second step") {
			steps {
				sh 'hostname'
			}
		}
/**		stage("Three step") {
			steps {
				ansiblePlaybook(
					playbook: '/etc/ansible/playbook_ats_test.yml',
					inventory: '/etc/ansible/hosts',
					colorized: true
				)
			}
		}*/
	}
}
